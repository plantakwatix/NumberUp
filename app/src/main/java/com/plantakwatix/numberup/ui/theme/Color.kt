package com.plantakwatix.numberup.ui.theme

import androidx.compose.ui.graphics.Color

// --- Palette Principale "Crème & Corail" ---

// FOND
val CreamBackground = Color(0xFFFDF6E3) // Un fond crème, doux et chaleureux

// TEXTE
val DarkCharcoalText = Color(0xFF333333) // Un gris très foncé pour le texte, plus doux que le noir pur

// ACCENTUATION PRINCIPALE (Corail)
val BrightCoral = Color(0xFFFD7E5D) // La couleur vive pour les boutons et éléments importants
val OnCoral = Color.White // Texte sur les éléments corail

// ACCENTUATION SECONDAIRE (Bleu Doux)
val SoftBlue = Color(0xFF6C9DAB) // Une couleur secondaire pour la diversité
val OnSoftBlue = Color.White

// SURFACE (pour les cartes, boîtes de dialogue, etc.)
val CardSurface = Color(0xFFFFFBF2) // Une version légèrement plus claire du fond pour un effet subtil

// BORDURES
val SubtleBorder = Color(0xFFE8E2D4) // Couleur pour les bordures de la grille